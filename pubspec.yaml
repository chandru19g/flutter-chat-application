name: worldchat
description: A new Application to chat with users all over the world
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: 1.0.3
  intl: ^0.17.0
  firebase_core: ^1.11.0
  cloud_firestore: ^3.1.6
  provider: ^6.0.2
  firebase_auth: ^3.3.5
  google_mobile_ads: ^0.13.4

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true